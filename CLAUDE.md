# Spec Instructions

Specs live in `.ai/specs/`. See `AGENTS.md` for the index.
Before implementing anything non-trivial, read the relevant spec.
For new features: draft spec in `.ai/changes/` first (see `/openspec-workflow` skill).
